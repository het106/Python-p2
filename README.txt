Setup of venv and how to run the kernel

python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

In jupyter notebook, change the kerner to the venv
This should now be able to run

Some installations you need to do beforehand:
pip install pandas
pip install numpy
pip isntall matplotlib
pip install seaborn
pip install ipywidgets